version: '3.8'

services:
  bot:
    environment:
      - WEATHER_TOKEN=b34c07d5728c6c70118dd27815f5e34c
      - TELEGRAM_TOKEN=5467370473:AAEdpHWrdCRHxdYJJpFjlhi07KYrRZ-lgV4 
      - MONGO_URI=mongodb://mongodb:27017/  
    build:
      context: .
      dockerfile: Dockerfile
    ports: 
      - "8080:8080"
    depends_on:
      - mongodb  

  mongodb:
    image: mongo:latest
    container_name: mongodb 
    ports:
      - "27100:27017" 
